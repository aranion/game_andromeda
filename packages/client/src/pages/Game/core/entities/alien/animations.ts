import { AnimationKey } from '../../constants';
import type { AlienAnimation } from './types';

export const alienAnimations: AlienAnimation = {
  [AnimationKey.AlienFly]: [
    [0, 0],
    [1, 0],
    [2, 0],
    [3, 0],
  ],
};
